@{
    Layout = null;
    var nombre     = ViewBag.Nombre as string ?? "";
    var apellido   = ViewBag.Apellido as string ?? "";
    var proyActivo = ViewBag.ProyectoActivo as string ?? "Sin proyecto";
    var inmuebles  = ViewBag.Inmuebles as List<dynamic> ?? new List<dynamic>();
    int total      = ViewBag.Total       != null ? (int)ViewBag.Total       : 0;
    int disponibles= ViewBag.Disponibles != null ? (int)ViewBag.Disponibles : 0;
    int reservados = ViewBag.Reservados  != null ? (int)ViewBag.Reservados  : 0;
    int vendidos   = ViewBag.Vendidos    != null ? (int)ViewBag.Vendidos    : 0;
    int enProceso  = ViewBag.EnProceso   != null ? (int)ViewBag.EnProceso   : 0;
    int la         = ViewBag.ListaActual != null ? (int)ViewBag.ListaActual  : 1;
    int miId       = ViewBag.IdUsuario   != null ? (int)ViewBag.IdUsuario    : 0;
    int proyId     = ViewBag.ProyectoId  != null ? (int)ViewBag.ProyectoId   : 0;
    var areaActual = ViewBag.AreaActual  as string ?? "";
    var grupos     = ViewBag.Grupos      as List<dynamic> ?? new List<dynamic>();
    var vendedores = ViewBag.Vendedores  as Dictionary<int,string> ?? new Dictionary<int,string>();
    var listasXArea = ViewBag.ListasXArea as Dictionary<string,int> ?? new Dictionary<string,int>();
    var aptsXArea   = ViewBag.AptsXArea   as Dictionary<string,int> ?? new Dictionary<string,int>();
    var iniciales   = ((nombre.Length>=1?nombre.Substring(0,1):"")+(apellido.Length>=1?apellido.Substring(0,1):"")).ToUpper();
}
<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="utf-8"/><meta name="viewport" content="width=device-width,initial-scale=1.0"/>
<title>Inmuebles · Londoño Gómez</title>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0;}
:root{--azul:#0076e3;--celeste:#00c9ff;--negro:#1e222b;--plata:#c3cfdb;--menta:#00d4a1;--mid:#0060bb;--blue:#0076e3;--rojo:#E63946;--verde:#34C759;--oro:#FF9500;--label:rgba(0,0,0,0.85);--label2:rgba(60,60,67,0.6);--label3:rgba(60,60,67,0.3);--sep:rgba(60,60,67,0.12);--sep2:rgba(60,60,67,0.06);--fill2:rgba(120,120,128,0.12);--fill3:rgba(120,120,128,0.08);--glass:rgba(255,255,255,0.75);--gb:rgba(255,255,255,0.6);--sh:0 8px 32px rgba(0,0,0,0.08),0 2px 8px rgba(0,0,0,0.04);--sh2:0 20px 60px rgba(0,0,0,0.10);}
html,body{background:#EEF2F7;color:var(--label);overflow-x:hidden;}
*{font-family:'Inter',system-ui,sans-serif;-webkit-font-smoothing:antialiased;}
body::before{content:'';position:fixed;inset:0;z-index:0;background:radial-gradient(ellipse 80% 60% at 10% 0%,rgba(0,118,227,0.12) 0%,transparent 55%),#EEF2F7;pointer-events:none;}
.app{display:flex;min-height:100vh;position:relative;z-index:1;}
.sidebar{width:230px;flex-shrink:0;background:var(--glass);backdrop-filter:blur(40px) saturate(180%);border-right:1px solid var(--gb);display:flex;flex-direction:column;position:sticky;top:0;height:100vh;overflow-y:auto;}
.sb-app{font-size:17px;font-weight:700;color:var(--label);letter-spacing:-0.4px;}
.sb-sub{font-size:11px;color:var(--label3);margin-top:1px;}
.sb-vendedor{margin:14px 10px 4px;background:linear-gradient(145deg,var(--azul),var(--blue));border-radius:16px;padding:14px;position:relative;overflow:hidden;}
.sb-vendedor::before{content:'';position:absolute;top:-20px;right:-20px;width:80px;height:80px;border-radius:50%;background:rgba(255,255,255,0.08);}
.sv-av{width:36px;height:36px;border-radius:50%;background:rgba(255,255,255,0.2);border:2px solid rgba(255,255,255,0.4);display:flex;align-items:center;justify-content:center;font-size:12px;font-weight:700;color:white;margin-bottom:6px;position:relative;z-index:1;}
.sv-nombre{font-size:13px;font-weight:700;color:white;position:relative;z-index:1;}
.sv-rol{font-size:10px;color:rgba(255,255,255,0.65);position:relative;z-index:1;}
.sb-proy{margin:8px 10px 0;background:rgba(0,118,227,0.08);border:1px solid rgba(0,118,227,0.18);border-radius:12px;padding:8px 12px;}
.sb-proy-lbl{font-size:9px;text-transform:uppercase;letter-spacing:0.1em;color:var(--label3);font-weight:600;margin-bottom:2px;}
.sb-proy-name{font-size:13px;font-weight:700;color:var(--azul);}
.pulse{width:5px;height:5px;border-radius:50%;background:#34C759;flex-shrink:0;animation:pring 2.5s ease infinite;display:inline-block;}
@@keyframes pring{0%{box-shadow:0 0 0 0 rgba(52,199,89,.4)}70%{box-shadow:0 0 0 5px rgba(52,199,89,0)}100%{box-shadow:0 0 0 0 rgba(52,199,89,0))}}
.sb-sec{padding:14px 16px 4px;font-size:10px;font-weight:600;text-transform:uppercase;letter-spacing:0.07em;color:var(--label3);}
.nav-item{display:flex;align-items:center;gap:10px;padding:9px 12px;margin:1px 6px;border-radius:11px;font-size:14px;color:var(--label2);text-decoration:none;transition:all 0.2s;}
.nav-item:hover{background:var(--fill3);color:var(--label);}
.nav-item.active{background:var(--blue);color:white;font-weight:600;box-shadow:0 4px 12px rgba(0,118,227,0.28);}
.nav-ico{width:26px;height:26px;border-radius:7px;display:flex;align-items:center;justify-content:center;font-size:13px;background:var(--fill3);flex-shrink:0;}
.nav-item.active .nav-ico{background:rgba(255,255,255,0.2);}
.sb-footer{margin-top:auto;padding:10px 12px 20px;border-top:1px solid var(--sep2);display:flex;align-items:center;gap:8px;}
.sb-av2{width:30px;height:30px;border-radius:50%;background:linear-gradient(135deg,var(--azul),var(--celeste));display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;color:white;}
.sb-logout{margin-left:auto;width:26px;height:26px;border-radius:7px;background:var(--fill3);display:flex;align-items:center;justify-content:center;font-size:13px;color:var(--label2);text-decoration:none;}
.sb-logout:hover{background:rgba(230,57,70,0.1);color:var(--rojo);}
.main{flex:1;display:flex;flex-direction:column;min-width:0;}
.topbar{height:50px;flex-shrink:0;background:var(--glass);backdrop-filter:blur(40px) saturate(180%);border-bottom:1px solid var(--sep);padding:0 18px;display:flex;align-items:center;justify-content:space-between;position:sticky;top:0;z-index:50;}
.tb-bread{font-size:13px;color:var(--label2);}.tb-bread strong{color:var(--label);font-weight:600;}
.tb-right{display:flex;align-items:center;gap:8px;}
.tb-chip{display:flex;align-items:center;gap:5px;background:var(--fill3);border:1px solid var(--sep);padding:4px 10px;border-radius:20px;font-size:12px;font-weight:600;color:var(--blue);}
.tb-ibtn{width:30px;height:30px;border-radius:9px;background:var(--fill3);display:flex;align-items:center;justify-content:center;font-size:14px;text-decoration:none;color:var(--label);}
.view{flex:1;padding:18px;display:flex;flex-direction:column;gap:14px;}
@@keyframes fu{from{opacity:0;transform:translateY(10px)}to{opacity:1;transform:translateY(0)}}
/* KPIs */
.kpi-row{display:grid;grid-template-columns:repeat(5,1fr);gap:12px;}
.kpi{background:var(--glass);backdrop-filter:blur(20px) saturate(180%);border:1px solid var(--gb);border-radius:18px;padding:16px;box-shadow:var(--sh);transition:transform 0.2s;animation:fu 0.35s ease both;}
.kpi:nth-child(1){animation-delay:.05s}.kpi:nth-child(2){animation-delay:.1s}.kpi:nth-child(3){animation-delay:.15s}.kpi:nth-child(4){animation-delay:.2s}.kpi:nth-child(5){animation-delay:.25s}
.kpi:hover{transform:translateY(-2px);}
.kpi-top{display:flex;justify-content:space-between;align-items:flex-start;margin-bottom:10px;}
.kpi-ico{width:34px;height:34px;border-radius:10px;display:flex;align-items:center;justify-content:center;font-size:17px;}
.kpi.k1 .kpi-ico{background:rgba(0,118,227,0.1)}.kpi.k2 .kpi-ico{background:rgba(52,199,89,0.1)}.kpi.k3 .kpi-ico{background:rgba(90,90,200,0.1)}.kpi.k4 .kpi-ico{background:rgba(255,149,0,0.1)}.kpi.k5 .kpi-ico{background:rgba(230,57,70,0.1)}
.kpi-tag{font-size:11px;font-weight:600;padding:3px 8px;border-radius:20px;}
.kpi.k1 .kpi-tag{background:rgba(0,118,227,0.1);color:var(--blue)}.kpi.k2 .kpi-tag{background:rgba(52,199,89,0.1);color:#1EA851}.kpi.k3 .kpi-tag{background:rgba(90,90,200,0.1);color:#5A5AC8}.kpi.k4 .kpi-tag{background:rgba(255,149,0,0.1);color:#CC7700}.kpi.k5 .kpi-tag{background:rgba(230,57,70,0.1);color:var(--rojo)}
.kpi-num{font-size:28px;font-weight:300;letter-spacing:-1.5px;line-height:1;margin-bottom:3px;}
.kpi.k1 .kpi-num{color:var(--azul)}.kpi.k2 .kpi-num{color:#1EA851}.kpi.k3 .kpi-num{color:#5A5AC8}.kpi.k4 .kpi-num{color:#CC7700}.kpi.k5 .kpi-num{color:var(--rojo)}
.kpi-lbl{font-size:12px;color:var(--label2);}
/* Áreas */
.areas-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(200px,1fr));gap:14px;animation:fu 0.35s ease both;}
.area-card{background:var(--glass);backdrop-filter:blur(20px) saturate(180%);border:2px solid var(--gb);border-radius:20px;padding:20px;cursor:pointer;transition:all 0.2s;box-shadow:var(--sh);text-decoration:none;display:block;}
.area-card:hover{border-color:var(--blue);transform:translateY(-3px);box-shadow:var(--sh2);}
.area-top{display:flex;align-items:center;justify-content:space-between;margin-bottom:12px;}
.area-metros{font-size:22px;font-weight:700;color:var(--azul);letter-spacing:-0.5px;}
.area-tipo-ext{background:rgba(255,149,0,0.12);color:#CC7700;font-size:11px;font-weight:700;padding:3px 9px;border-radius:20px;}
.area-tipo-int{background:rgba(0,118,227,0.1);color:var(--blue);font-size:11px;font-weight:700;padding:3px 9px;border-radius:20px;}
.area-precio-lbl{font-size:10px;color:var(--label3);font-weight:600;text-transform:uppercase;margin-bottom:2px;}
.area-precio{font-size:17px;font-weight:600;color:var(--azul);margin-bottom:10px;}
.area-listas{display:flex;gap:6px;flex-wrap:wrap;margin-bottom:12px;}
.area-lista-chip{font-size:10px;font-weight:600;padding:2px 7px;border-radius:10px;background:var(--fill3);color:var(--label2);}
.area-lista-chip.activa{background:rgba(0,118,227,0.1);color:var(--blue);}
.prog-wrap{margin-bottom:4px;}
.prog-bar{height:6px;border-radius:10px;background:var(--fill2);overflow:hidden;}
.prog-fill{height:100%;border-radius:10px;background:linear-gradient(90deg,var(--verde),#1EA851);}
.prog-fill.naranja{background:linear-gradient(90deg,var(--oro),#CC7700);}
.prog-fill.rojo{background:linear-gradient(90deg,var(--rojo),#B02030);}
.prog-lbl{display:flex;justify-content:space-between;font-size:11px;color:var(--label2);margin-top:3px;}
/* Tabla */
.card{background:var(--glass);backdrop-filter:blur(20px) saturate(180%);border:1px solid var(--gb);border-radius:18px;overflow:hidden;box-shadow:var(--sh);animation:fu 0.35s ease 0.1s both;}
.card-head{display:flex;align-items:center;justify-content:space-between;padding:12px 16px;border-bottom:1px solid var(--sep2);flex-wrap:wrap;gap:8px;}
.card-title{font-size:15px;font-weight:600;color:var(--label);}
.tbl-wrap{overflow-x:auto;}
.tbl{width:100%;border-collapse:collapse;min-width:560px;}
.tbl thead{position:sticky;top:0;z-index:2;background:rgba(255,255,255,0.95);}
.tbl th{font-size:11px;font-weight:600;text-transform:uppercase;letter-spacing:0.06em;color:var(--label3);padding:10px 16px;text-align:left;border-bottom:1px solid var(--sep2);}
.tbl td{padding:11px 16px;font-size:13px;color:var(--label);border-bottom:1px solid var(--sep2);}
.tbl tr:last-child td{border-bottom:none;}
.tbl tr:hover td{background:var(--fill3);}
.bdg{display:inline-flex;align-items:center;gap:4px;padding:3px 9px;border-radius:20px;font-size:11px;font-weight:600;}
.bdg::before{content:'';width:5px;height:5px;border-radius:50%;flex-shrink:0;}
.bdg.D{background:rgba(52,199,89,0.12);color:#1EA851}.bdg.D::before{background:#34C759}
.bdg.V{background:rgba(230,57,70,0.12);color:var(--rojo)}.bdg.V::before{background:var(--rojo)}
.bdg.R{background:rgba(255,149,0,0.12);color:#CC7700}.bdg.R::before{background:var(--oro)}
.bdg.P{background:rgba(90,90,200,0.12);color:#5A5AC8}.bdg.P::before{background:#5A5AC8}
.filter-bar{display:flex;align-items:center;gap:8px;flex-wrap:wrap;}
.search-wrap{position:relative;max-width:220px;}
.search-wrap input{width:100%;padding:7px 12px 7px 30px;background:var(--fill3);border:1px solid var(--sep);border-radius:10px;font-size:13px;color:var(--label);outline:none;}
.search-wrap input:focus{background:white;border-color:var(--blue);}
.search-ico{position:absolute;left:9px;top:50%;transform:translateY(-50%);font-size:12px;color:var(--label3);pointer-events:none;}
.seg{display:flex;background:var(--fill3);border-radius:10px;padding:3px;gap:2px;}
.seg-btn{padding:5px 10px;border-radius:7px;font-size:12px;font-weight:600;color:var(--label2);cursor:pointer;border:none;background:none;transition:all 0.2s;}
.seg-btn.active{background:white;color:var(--label);box-shadow:0 1px 4px rgba(0,0,0,0.1);}
.btn-vender{background:var(--blue);color:white;border:none;padding:5px 12px;border-radius:8px;font-size:12px;font-weight:600;cursor:pointer;transition:all 0.2s;}
.btn-vender:hover{background:var(--mid);}
.btn-reservar{background:rgba(255,149,0,0.12);color:#CC7700;border:none;padding:5px 10px;border-radius:8px;font-size:12px;font-weight:600;cursor:pointer;margin-left:4px;}
.btn-cancelar{background:rgba(230,57,70,0.1);color:var(--rojo);border:none;padding:5px 10px;border-radius:8px;font-size:12px;font-weight:600;cursor:pointer;}
.tag-yo{font-size:11px;color:#5A5AC8;font-weight:600;margin-right:4px;}
.tag-otro{font-size:11px;color:var(--label3);}
.area-volver{display:inline-flex;align-items:center;gap:6px;background:var(--fill3);border:1px solid var(--sep);color:var(--label);padding:7px 14px;border-radius:10px;font-size:13px;font-weight:600;text-decoration:none;}
.area-volver:hover{background:var(--fill2);}
.alerta-exito{background:rgba(52,199,89,0.1);border:1px solid rgba(52,199,89,0.3);border-radius:12px;padding:10px 16px;font-size:13px;font-weight:600;color:#1EA851;}
.alerta-error{background:rgba(230,57,70,0.1);border:1px solid rgba(230,57,70,0.3);border-radius:12px;padding:10px 16px;font-size:13px;font-weight:600;color:var(--rojo);}
.empty{padding:48px;text-align:center;}
.empty-ico{font-size:40px;opacity:0.4;margin-bottom:8px;}
.empty-txt{font-size:15px;font-weight:600;color:var(--label2);}
.hamburger{display:none;flex-direction:column;gap:5px;cursor:pointer;padding:6px;border-radius:8px;background:var(--fill3);border:none;}
.hamburger span{display:block;width:18px;height:2px;background:var(--label);border-radius:2px;}
@@media(max-width:1200px){.kpi-row{grid-template-columns:repeat(3,1fr)!important;}.areas-grid{grid-template-columns:repeat(3,1fr)!important;}}
@@media(max-width:1024px){.kpi-row{grid-template-columns:repeat(2,1fr)!important;}.areas-grid{grid-template-columns:repeat(2,1fr)!important;}}
@@media(max-width:700px){
    html,body{height:auto!important;overflow:auto!important;}
    .app{flex-direction:column!important;}
    .sidebar{position:fixed!important;top:0!important;left:-270px!important;width:260px!important;height:100vh!important;z-index:100!important;overflow-y:auto!important;transition:left 0.3s!important;}
    .sidebar.open{left:0!important;}
    .main{width:100%!important;}
    .hamburger{display:flex!important;}
    .tb-chip{display:none!important;}
    .kpi-row{grid-template-columns:1fr 1fr!important;gap:8px!important;}
    .areas-grid{grid-template-columns:1fr 1fr!important;}
}

        /* ── Sidebar blanco corporativo ── */
        .sidebar{background:#fff!important;border-right:1px solid rgba(0,0,0,0.07)!important;backdrop-filter:none!important;box-shadow:2px 0 12px rgba(0,0,0,0.04)!important;}
        .sb-logo-area{padding:16px 14px 12px;border-bottom:1px solid rgba(0,0,0,0.06);margin-bottom:4px;}
        .sb-logo-area img{width:100%;max-height:52px;object-fit:contain;display:block;mix-blend-mode:multiply;filter:contrast(1.05);}
        .sb-app{color:#1e222b!important;font-weight:700!important;}
        .sb-sub{color:rgba(30,34,43,0.45)!important;}
        .sb-proy{background:rgba(0,118,227,0.06)!important;border-color:rgba(0,118,227,0.18)!important;}
        .sb-proy:hover{background:rgba(0,118,227,0.10)!important;}
        .sb-proy-lbl{color:rgba(30,34,43,0.45)!important;font-size:9px!important;}
        .sb-proy-name{color:#0076e3!important;font-weight:700!important;}
        .sb-proy-meta{color:rgba(30,34,43,0.5)!important;}
        .proy-drop{background:#fff!important;border-color:rgba(0,0,0,0.09)!important;box-shadow:0 8px 24px rgba(0,0,0,0.1)!important;}
        .proy-drop-item{color:#1e222b!important;}
        .proy-drop-item:hover{background:rgba(0,118,227,0.06)!important;color:#0076e3!important;}
        .proy-drop-link{color:#0076e3!important;background:rgba(0,118,227,0.04)!important;}
        .sb-sec{color:rgba(30,34,43,0.38)!important;letter-spacing:0.08em!important;}
        .nav-item{color:rgba(30,34,43,0.65)!important;}
        .nav-item:hover{background:rgba(0,118,227,0.07)!important;color:#0076e3!important;}
        .nav-item.active{background:#0076e3!important;color:#fff!important;}
        .nav-item.active .nav-ico{background:rgba(255,255,255,0.22)!important;}
        .nav-ico{background:rgba(30,34,43,0.06)!important;color:inherit!important;}
        .sb-footer{border-top-color:rgba(0,0,0,0.07)!important;}
        .sb-logout{background:rgba(30,34,43,0.05)!important;color:rgba(30,34,43,0.45)!important;}
        .sb-logout:hover{background:rgba(230,57,70,0.08)!important;color:#E63946!important;}
        .nav-item{display:flex!important;align-items:center!important;gap:10px!important;padding:9px 12px!important;margin:1px 6px!important;border-radius:11px!important;font-size:14px!important;min-height:40px!important;text-decoration:none!important;transition:all 0.18s!important;}
        .nav-ico{width:26px!important;height:26px!important;border-radius:8px!important;display:flex!important;align-items:center!important;justify-content:center!important;font-size:14px!important;flex-shrink:0!important;}
        .sb-sec{padding:14px 18px 4px!important;font-size:10px!important;font-weight:600!important;text-transform:uppercase!important;}
    </style>
</head>
<body>
<div class="app">
<div class="sidebar" id="sidebar">
        <div class="sb-logo-area">
            <img src="~/Images/Logo azul 2.jpg" alt="Londoño Gómez" style="width:100%;max-height:56px;object-fit:contain;display:block;mix-blend-mode:multiply;" />
        </div>
    <div class="sb-vendedor">
        <div class="sv-av">@iniciales</div>
        <div class="sv-nombre">@nombre @apellido</div>
        <div class="sv-rol">Vendedor</div>
    </div>
    <div class="sb-proy">
        <div class="sb-proy-lbl"><span class="pulse"></span> Proyecto activo</div>
        <div class="sb-proy-name">@proyActivo</div>
    </div>
    <div class="sb-sec">Mi espacio</div>
    <a class="nav-item" href="/Vendedor"><div class="nav-ico">⊞</div>Inicio</a>
    <a class="nav-item active" href="/Vendedor/Inmuebles"><div class="nav-ico">🏢</div>Inmuebles</a>
    <a class="nav-item " href="/Vendedor/MisReservas"><div class="nav-ico">🔒</div>Mis reservas</a>
    <a class="nav-item" href="/Vendedor/MisVentas"><div class="nav-ico">💲</div>Mis ventas</a>
    <a class="nav-item" href="/Vendedor/Perfil"><div class="nav-ico">👤</div>Mi perfil</a>
    <div class="sb-footer">
        <div class="sb-av2">@iniciales</div>
        <div><div style="font-size:12px;font-weight:600">@nombre</div><div style="font-size:10px;color:var(--label3)">Vendedor</div></div>
        <a class="sb-logout" href="/Account/Logout">↩</a>
    </div>
</div>
<div class="main">
    <div class="topbar">
        <div style="display:flex;align-items:center;gap:10px;">
            <button class="hamburger" onclick="toggleSidebar()"><span></span><span></span><span></span></button>
            <span class="tb-bread">
                <strong>Inmuebles</strong>
                @if (!string.IsNullOrEmpty(areaActual)) { <span> · @areaActual m²</span> }
                · @proyActivo
            </span>
        </div>
        <div class="tb-right"><div class="tb-chip"><span class="pulse"></span>@proyActivo</div><a class="tb-ibtn" href="/Account/Logout">↩</a></div>
    </div>
    <div class="view">
        @if (TempData["Exito"] != null) { <div class="alerta-exito">✅ @TempData["Exito"]</div> }
        @if (TempData["Error"] != null) { <div class="alerta-error">❌ @TempData["Error"]</div> }

        <!-- KPIs globales -->
        <div class="kpi-row">
            <div class="kpi k1"><div class="kpi-top"><div class="kpi-ico">🏠</div><span class="kpi-tag">Total</span></div><div class="kpi-num">@total</div><div class="kpi-lbl">Inmuebles</div></div>
            <div class="kpi k2"><div class="kpi-top"><div class="kpi-ico">✅</div><span class="kpi-tag">Disponibles</span></div><div class="kpi-num">@disponibles</div><div class="kpi-lbl">Para venta</div></div>
            <div class="kpi k3"><div class="kpi-top"><div class="kpi-ico">⏳</div><span class="kpi-tag">En proceso</span></div><div class="kpi-num">@enProceso</div><div class="kpi-lbl">Gestionando</div></div>
            <div class="kpi k4"><div class="kpi-top"><div class="kpi-ico">🔒</div><span class="kpi-tag">Reservados</span></div><div class="kpi-num">@reservados</div><div class="kpi-lbl">Bloqueados</div></div>
            <div class="kpi k5"><div class="kpi-top"><div class="kpi-ico">🔑</div><span class="kpi-tag">Vendidos</span></div><div class="kpi-num">@vendidos</div><div class="kpi-lbl">Cerrados</div></div>
        </div>

        @if (string.IsNullOrEmpty(areaActual)) {
            <!-- PANTALLA DE ÁREAS -->
            <div style="font-size:15px;font-weight:600;color:var(--label)">Selecciona un área para ver los inmuebles</div>
            <div class="areas-grid">
                @foreach (var g in grupos) {
                    var porcVend  = g.Total > 0 ? (int)Math.Round((double)g.Vendidos / g.Total * 100) : 0;
                    var fillCls   = porcVend >= 80 ? "rojo" : porcVend >= 50 ? "naranja" : "";
                    var tipoLabel = g.Tipo?.ToString() ?? "";
                    var tipoClass = tipoLabel == "EXT" ? "area-tipo-ext" : "area-tipo-int";
                    var metrosStr = g.Metros.ToString();
                    var laArea    = listasXArea.ContainsKey(metrosStr) ? listasXArea[metrosStr] : la;
                    long p1=(long)g.PrecioL1; long p2=(long)g.PrecioL2; long p3=(long)g.PrecioL3;
                    long p4=(long)g.PrecioL4; long p5=(long)g.PrecioL5;
                    long precActArea = laArea==1?p1:laArea==2?p2:laArea==3?p3:laArea==4?p4:p5;
                    if (precActArea == 0) precActArea = (long)g.PrecioActivo;
                    <a href="/Vendedor/Inmuebles?area=@Uri.EscapeDataString(g.Metros)" class="area-card">
                        <div class="area-top">
                            <div class="area-metros">@g.Metros <span style="font-size:13px;font-weight:400;color:var(--label2)">m²</span></div>
                            <span class="@tipoClass">@tipoLabel</span>
                        </div>
                        <span style="display:inline-block;background:linear-gradient(135deg,var(--azul),var(--celeste));color:white;font-size:11px;font-weight:700;padding:3px 10px;border-radius:20px;margin-bottom:8px;">Lista @laArea activa</span>
                        <div class="area-precio-lbl">Precio lista @laArea</div>
                        <div class="area-precio">$@string.Format("{0:N0}", precActArea)</div>
                        <div class="area-listas">
                            @for (int ln = 1; ln <= 5; ln++) {
                                long pl = ln==1?g.PrecioL1:ln==2?g.PrecioL2:ln==3?g.PrecioL3:ln==4?g.PrecioL4:g.PrecioL5;
                                if (pl > 0) {
                                    var chipCls = ln == laArea ? "area-lista-chip activa" : "area-lista-chip";
                                    <span class="@chipCls">L@(ln): $@string.Format("{0:N0}", pl)</span>
                                }
                            }
                        </div>
                        <div class="prog-wrap">
                            <div class="prog-bar"><div class="prog-fill @fillCls" style="width:@(porcVend)%"></div></div>
                            <div class="prog-lbl">
                                <span>@g.Disponibles disp · @g.Vendidos vend</span>
                                <span>@(porcVend)%</span>
                            </div>
                        </div>
                    </a>
                }
            </div>
        } else {
            <!-- TABLA FILTRADA POR ÁREA -->
             var laEstaArea = listasXArea.ContainsKey(areaActual) ? listasXArea[areaActual] : la; 
            <div style="display:flex;align-items:center;gap:10px;flex-wrap:wrap;">
                <a href="/Vendedor/Inmuebles" class="area-volver">← Todas las áreas</a>
                <span style="font-size:15px;font-weight:600;color:var(--label)">@areaActual m² · @inmuebles.Count inmuebles</span>
            </div>
            <div class="card">
                <div class="card-head">
                    <span class="card-title">@areaActual m² · Lista @laEstaArea</span>
                    <div class="filter-bar">
                        <div class="search-wrap"><span class="search-ico">🔍</span><input type="text" id="buscar" placeholder="Buscar apto, piso..." oninput="filtrar()"/></div>
                        <div class="seg">
                            <button class="seg-btn active" onclick="setFiltro('todos',this)">Todos</button>
                            <button class="seg-btn" onclick="setFiltro('DISPONIBLE',this)">Disponibles</button>
                            <button class="seg-btn" onclick="setFiltro('RESERVADO',this)">Reservados</button>
                        </div>
                    </div>
                </div>
                @if (inmuebles.Count > 0) {
                    <div class="tbl-wrap">
                        <table class="tbl" id="tablaInmuebles">
                            <thead><tr>
                                <th>Apto</th><th>Tipo</th><th>Piso</th><th>Metros</th>
                                <th>Precio lista @laEstaArea</th><th>Torre</th><th>Estado</th><th>Acción</th>
                            </tr></thead>
                            <tbody>
                            @foreach (var i in inmuebles) {
                                var cls = i.Estado == "DISPONIBLE" ? "D" : i.Estado == "VENDIDO" ? "V" : i.Estado == "EN PROCESO" ? "P" : "R";
                                var plRaw = la==1?i.Lista1:la==2?i.Lista2:la==3?i.Lista3:la==4?i.Lista4:i.Lista5;
                                var plLimpio = (plRaw?.ToString()??"0").Replace("$","").Replace(".","").Replace(",","").Replace(" ","").Trim();
                                <tr data-estado="@i.Estado">
                                    <td style="font-weight:600">@i.Apto</td>
                                    <td>
                                        @if (i.Tipo == "EXT") {
                                            <span style="background:rgba(255,149,0,0.12);color:#CC7700;font-size:11px;font-weight:700;padding:2px 8px;border-radius:20px">EXT</span>
                                        } else if (i.Tipo == "INT") {
                                            <span style="background:rgba(0,118,227,0.1);color:var(--blue);font-size:11px;font-weight:700;padding:2px 8px;border-radius:20px">INT</span>
                                        } else { <span>@i.Tipo</span> }
                                    </td>
                                    <td>@i.Piso</td>
                                    <td>@i.Metros m²</td>
                                    <td style="font-weight:500;color:var(--azul)">$@(string.Format("{0:N0}", long.TryParse(plLimpio, out long lp) ? lp : 0))</td>
                                    <td>@i.Torre</td>
                                    <td>
                                        <span class="bdg @cls">@i.Estado</span>
                                        @if (i.Estado == "EN PROCESO" && i.IdVendedorEnProceso > 0) {
                                            var nomVend = vendedores.ContainsKey(i.IdVendedorEnProceso) ? vendedores[i.IdVendedorEnProceso] : "Vendedor";
                                            <br/><span style="font-size:11px;color:#5A5AC8;font-weight:600">👤 @nomVend</span>
                                        }
                                    </td>
                                    <td>
                                        @if (i.Estado == "DISPONIBLE") {
                                            <form action="/Vendedor/TomarInmueble" method="post" style="margin:0;display:inline">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="idInmueble" value="@i.Id"/>
                                                <button type="submit" class="btn-vender">Vender</button>
                                            </form>
                                            <form action="/Vendedor/ReservarInmueble" method="post" style="margin:0;display:inline">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="idInmueble" value="@i.Id"/>
                                                <button type="submit" class="btn-reservar" onclick="return confirm('¿Reservar?')">Reservar</button>
                                            </form>
                                        } else if (i.Estado == "EN PROCESO" && i.IdVendedorEnProceso == miId) {
                                            <span class="tag-yo">Tú lo tienes</span>
                                            <form action="/Vendedor/CancelarProceso" method="post" style="margin:0;display:inline">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="idInmueble" value="@i.Id"/>
                                                <button type="submit" class="btn-cancelar">Cancelar</button>
                                            </form>
                                        } else if (i.Estado == "EN PROCESO") {
                                            var nomVend2 = vendedores.ContainsKey(i.IdVendedorEnProceso) ? vendedores[i.IdVendedorEnProceso] : "Otro vendedor";
                                            <span class="tag-otro">⏳ @nomVend2</span>
                                        } else if (i.Estado == "RESERVADO" && i.IdVendedorReserva == miId) {
                                            <span class="tag-yo">Tú lo reservaste</span>
                                            <form action="/Vendedor/LiberarReserva" method="post" style="margin:0;display:inline">
                                                @Html.AntiForgeryToken()
                                                <input type="hidden" name="idInmueble" value="@i.Id"/>
                                                <button type="submit" class="btn-cancelar" onclick="return confirm('¿Liberar reserva?')">Liberar</button>
                                            </form>
                                        } else {
                                            <span style="color:var(--label3);font-size:12px">—</span>
                                        }
                                    </td>
                                </tr>
                            }
                            </tbody>
                        </table>
                    </div>
                } else {
                    <div class="empty"><div class="empty-ico">🏢</div><div class="empty-txt">No hay inmuebles disponibles en esta área</div></div>
                }
            </div>
        }
    </div>
</div>
</div>
<div id="sbOverlay" onclick="closeSidebar()" style="display:none;position:fixed;inset:0;background:rgba(0,0,0,0.4);z-index:99;backdrop-filter:blur(2px);"></div>
<script>
    let filtroActual = 'todos';
    function setFiltro(f,btn){filtroActual=f;document.querySelectorAll('.seg-btn').forEach(b=>b.classList.remove('active'));btn.classList.add('active');filtrar();}
    function filtrar(){
        const q=document.getElementById('buscar')?.value.toLowerCase()||'';
        document.querySelectorAll('#tablaInmuebles tbody tr').forEach(tr=>{
            const matchF=filtroActual==='todos'||tr.dataset.estado===filtroActual;
            tr.style.display=matchF&&tr.innerText.toLowerCase().includes(q)?'':'none';
        });
    }
    function toggleSidebar(){var sb=document.getElementById('sidebar'),ov=document.getElementById('sbOverlay');if(!sb)return;var o=sb.classList.toggle('open');if(ov)ov.style.display=o?'block':'none';}
    function closeSidebar(){var sb=document.getElementById('sidebar'),ov=document.getElementById('sbOverlay');if(sb)sb.classList.remove('open');if(ov)ov.style.display='none';}
</script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/7.0.5/signalr.min.js"></script>
<script>
    const proyIdSR = @proyId;
    const miIdSR   = @miId;
    const conn = new signalR.HubConnectionBuilder().withUrl("/ventasHub").withAutomaticReconnect().build();
    conn.on("ListaActualizada", function(proyIdEvt, nuevaLista) {
        if (proyIdEvt !== proyIdSR) return;
        mostrarToast('📋 Lista del proyecto actualizada a ' + nuevaLista, 'info');
        setTimeout(function(){ location.reload(); }, 2000);
    });

    // Cuando el admin cambia la lista de un área específica
    conn.on("ListaAreaActualizada", function(proyIdEvt, metros, nuevaLista) {
        if (proyIdEvt !== proyIdSR) return;
        mostrarToast('📋 Área ' + metros + ' m² → Lista ' + nuevaLista, 'info');
        setTimeout(function(){ location.reload(); }, 2000);
    });
    conn.on("InmuebleActualizado", function(idInmueble, nuevoEstado) {
        var ico = nuevoEstado === 'DISPONIBLE' ? '✅' : nuevoEstado === 'VENDIDO' ? '🔑' : '⏳';
        mostrarToast(ico + ' Inmueble: ' + nuevoEstado, 'info');
    });
    conn.start().catch(function(err){ console.error('SignalR:', err); });
    function mostrarToast(msg, tipo) {
        var t=document.createElement('div');
        t.style.cssText='position:fixed;bottom:24px;right:24px;z-index:9999;background:'+(tipo==='exito'?'#1EA851':'var(--blue)')+';color:white;padding:12px 20px;border-radius:12px;font-size:13px;font-weight:600;box-shadow:0 8px 24px rgba(0,0,0,0.2);';
        t.textContent=msg;document.body.appendChild(t);setTimeout(function(){t.remove();},3500);
    }
</script>
</body>
</html>